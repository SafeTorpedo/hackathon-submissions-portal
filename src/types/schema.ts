//define form data type
type schema = {
    coverImage: string;
    title: string;
    summary: string;
    id: string;
    description: string;
    name: string;
    startDate: string;
    endDate: string;
    github: string;
    other: string;
    fav: boolean;
};

export default schema;
